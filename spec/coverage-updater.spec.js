const coverageUpdater = require('../index.js');

console.log('ron coverage-updater.spec.()', coverageUpdater);// TODO remove this

describe('coverage-updater spec', () => {
  it('should pass', () => {
    expect(true).toBe(true);
  });
});
