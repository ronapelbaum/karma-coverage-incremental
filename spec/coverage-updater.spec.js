const coverage_updater = require('../index')
console.log('ron coverage-updater.spec.()', coverage_updater);//TODO remove this

describe('coverage-updater spec', ()=>{
    it('should fail', ()=>{
        expect(true).toBe(false);
    });
});
